import json
import auth
import retrieval
import manipulation



#  Must do authentication first, must have available access token (one available to be retrieved based on data in .env)
#  Can either use user-authorized token, which has some special stuff to do, or for searching basic public data can use a basic token at any time


